{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Product Page!</h1>
        <h3>{{ form }}</h3>
        <ul>
            <h4>{{ text }}</h4>
        </ul>
        <ul>
            <li><a href="{{ path('product_add') }}"> Add new product </a></li>
            <li><a href="{{ path('product_name_search') }}"> Find some product </a></li>
            <li><a href="{{ path('product_update') }}"> Update product </a></li>
            <li><a href="{{ path('product_delete') }}"> Delete product </a></li>
            <li><a href="{{ path('product_get_all') }}"> Get all info about products </a></li>
        </ul>
    </div>
{% endblock %}

